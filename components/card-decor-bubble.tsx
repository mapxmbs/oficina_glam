/**
 * @deprecated Ornamentos decorativos removidos do design system.
 * Componente mantido para compatibilidade – renderiza nada.
 */
type Variant = 'dark' | 'light' | 'dica';

export default function CardDecorBubble(_props?: { variant?: Variant }) {
  return null;
}
